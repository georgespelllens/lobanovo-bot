# 🐺 Приёмная Лобанова — ИИ-наставник по личному бренду

Telegram-бот + веб-дашборд — масштабируемая система доступа к наставничеству Константина Лобанова.

**Бот:** [@lobanov_mentor_bot](https://t.me/lobanov_mentor_bot)
**Веб:** [lobanov-mentor.up.railway.app](https://lobanov-mentor.up.railway.app)

---

## Что умеет

### 💬 RAG Q&A
Отвечает на вопросы о личном бренде, карьере и контенте, опираясь на 3000+ постов Лобанова. Использует векторный поиск (pgvector) для нахождения релевантных постов и формирует ответы в стиле Лобанова.

### 📝 Аудит постов
Разбирает текст пользователя по 6 критериям Лобанова: мета-сообщение, конкретика, позиционирование, читабельность, антипаттерны, CTA. Может предложить переписанный вариант.

### 📋 Еженедельные задания + прогресс
Персонализированные задания на неделю с трекингом XP и уровнями: 🐱 Котёнок (0–99 XP) → 🐺 Волчонок (100–299 XP) → 🐺🔥 Волк (300+ XP). Задания приходят каждый понедельник в 10:00 МСК.

### 🎤 Прямая линия с Костей
Киллер-фича: за 1000₽ пользователь задаёт вопрос, Лобанов отвечает голосовым на 5–10 минут. Полный цикл: оплата → вопрос → карточка админу → голосовой ответ → доставка. Лучшие ответы (с согласия) попадают в базу знаний.

### 🔔 Эскалация к Лобанову
Бот распознаёт, когда нужен живой человек (прямой запрос, 3x 👎, сложный кейс) и предлагает Прямую линию или полную консультацию.

### 💎 Подписки
Free (0₽) → Pro (990₽/мес) → Premium (4990₽/мес) с разными лимитами на вопросы, аудиты и доступ к дашборду.

### 🌐 Веб-дашборд
- **Пользователь:** прогресс, история, задания, аудиты
- **Админ:** статистика, пользователи, эскалации, Прямая линия, база знаний, финансы

---

## Команды бота

| Команда | Действие |
|---------|----------|
| `/start` | Приветствие + онбординг (5 вопросов) |
| `/help` | Список возможностей |
| `/ask` | Режим Q&A |
| `/audit` | Режим аудита постов |
| `/tasks` | Задания на неделю |
| `/progress` | Уровень, XP, статистика |
| `/ask_kostya` | 🎤 Прямая линия (1000₽) |
| `/consult` | Записаться на консультацию |
| `/plan` | Тариф и лимиты |
| `/feedback` | Отзыв или предложение |

**Админ-команды:** `/admin_stats`, `/admin_escalations`, `/admin_top_questions`, `/admin_users`, `/admin_broadcast`, `/admin_add_knowledge`

---

## Технический стек

| Компонент | Технология |
|-----------|------------|
| Язык | Python 3.11+ |
| Бот | python-telegram-bot 21.x |
| Веб | FastAPI + Jinja2 + HTMX |
| БД | PostgreSQL + pgvector |
| ORM | SQLAlchemy 2.0 + asyncpg |
| Миграции | Alembic |
| LLM | OpenRouter (Claude Sonnet 4 / Haiku 4.5) |
| STT | OpenAI Whisper |
| Деплой | Railway.com |

---

## Структура проекта

```
lobanovo-bot/
├── src/
│   ├── main.py                     # FastAPI + Telegram webhook
│   ├── config.py                   # Pydantic Settings + тарифы + модели
│   │
│   ├── bot/
│   │   ├── handlers/
│   │   │   ├── start.py            # /start, онбординг 5 вопросов
│   │   │   ├── qa.py               # RAG Q&A с оценками 👍/👎
│   │   │   ├── audit.py            # Аудит постов по 6 критериям
│   │   │   ├── tasks.py            # Задания, /progress
│   │   │   ├── admin.py            # Админ-команды
│   │   │   ├── payment.py          # Тарифы, оплата подписок
│   │   │   ├── direct_line.py      # 🎤 Прямая линия с Костей
│   │   │   └── voice.py            # Голосовые → Whisper → текст
│   │   ├── callbacks.py            # Роутинг inline-кнопок
│   │   ├── middleware.py           # Трекинг пользователей
│   │   └── scheduler.py            # Cron: задания ПН, напоминания ВС, дедлайны
│   │
│   ├── services/
│   │   ├── llm_service.py          # OpenRouter API + fallback
│   │   ├── rag_service.py          # Векторный поиск + системные промпты
│   │   ├── stt_service.py          # Whisper транскрипция
│   │   ├── task_service.py         # XP, уровни, задания
│   │   ├── escalation_service.py   # Эскалации к Лобанову
│   │   ├── direct_line_service.py  # Прямая линия: очередь, карточки, KB
│   │   └── subscription_service.py # Тарифы, лимиты
│   │
│   ├── database/
│   │   ├── models.py               # 10 SQLAlchemy моделей
│   │   ├── connection.py           # Async session management
│   │   └── repository.py           # CRUD операции
│   │
│   ├── web/
│   │   ├── routes/                 # auth, dashboard, admin
│   │   ├── templates/              # Jinja2 (base, dashboard, admin, login)
│   │   └── static/css/             # Тёмная тема
│   │
│   └── utils/                      # Логгер, валидаторы
│
├── scripts/
│   ├── load_knowledge_base.py      # Парсинг HTML/MD экспортов каналов
│   ├── generate_embeddings.py      # Эмбеддинги + категоризация LLM
│   └── seed_tasks.py               # 21 шаблон заданий (7 на уровень)
│
├── migrations/                     # Alembic
├── requirements.txt
├── Procfile                        # Railway: web + release
└── .env.example
```

---

## Запуск

### Переменные окружения

```bash
cp .env.example .env
# Заполни: TELEGRAM_BOT_TOKEN, DATABASE_URL, OPENROUTER_API_KEY,
#          OPENAI_API_KEY, ADMIN_CHAT_ID, ADMIN_USER_IDS
```

### Локально

```bash
# Виртуальное окружение
python3 -m venv venv && source venv/bin/activate

# Зависимости
pip install -r requirements.txt

# PostgreSQL с pgvector (Docker)
docker run -d --name lobanov-pg \
  -e POSTGRES_PASSWORD=password -e POSTGRES_DB=lobanov_bot \
  -p 5432:5432 pgvector/pgvector:pg16

# Миграции
alembic upgrade head

# Запуск
python -m uvicorn src.main:app --reload --port 8000
```

### Загрузка базы знаний

```bash
# Посты из экспортов каналов (положить файлы в data/)
python scripts/load_knowledge_base.py --all

# Эмбеддинги + категоризация
python scripts/generate_embeddings.py

# Шаблоны заданий
python scripts/seed_tasks.py
```

### Деплой на Railway

1. New Project → Deploy from GitHub → `georgespelllens/lobanovo-bot`
2. Add → Database → PostgreSQL
3. Добавить все переменные из `.env` в Variables
4. Railway автоматически задеплоит (Procfile + release migration)

---

## Архитектура

```
Telegram User ──→ Webhook (/webhook)
                    │
                    ├── Command Router ──→ /start, /help, /audit, /ask_kostya...
                    ├── Message Router ──→ Q&A / Audit / Task Review / Direct Line
                    ├── Voice Handler  ──→ Whisper STT → Text → Router
                    └── Callback Router ──→ 👍/👎, оплата, онбординг, админ
                    
Q&A Pipeline:
  Question → Embedding → pgvector search (top-5) → System prompt + context → Claude Sonnet → Response

Audit Pipeline:
  Post text → 6 criteria prompt → Claude Sonnet → Structured review

Direct Line Pipeline:
  /ask_kostya → Payment → Question → Admin card → Lobanov voice → Deliver → KB enrichment
```

---

## База данных (10 таблиц)

- `users` — пользователи, онбординг, XP, подписки, лимиты
- `knowledge_base` — 3000+ постов + pgvector эмбеддинги
- `conversations` — сессии общения (Q&A, аудит, онбординг)
- `messages` — все сообщения + оценки + стоимость API
- `task_templates` — 21 шаблон заданий по уровням
- `user_tasks` — назначенные задания + ревью
- `escalations` — эскалации к Лобанову
- `subscriptions` — история подписок
- `system_prompts` — промпты для разных режимов
- `direct_questions` — Прямая линия (полный жизненный цикл)
- `feedback` — отзывы пользователей

---

## Лицензия

Proprietary. © 2026 Константин Лобанов.
