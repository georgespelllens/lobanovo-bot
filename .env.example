# Telegram
TELEGRAM_BOT_TOKEN=<токен от @BotFather>
ADMIN_CHAT_ID=<ID чата/канала для уведомлений админу>
ADMIN_USER_IDS=<telegram_id Лобанова через запятую>

# xAI (Grok) — chat completions
XAI_API_KEY=<ключ из https://console.x.ai>

# OpenRouter — embeddings only (google/gemini-embedding-001)
OPENROUTER_API_KEY=<ключ от OpenRouter>

# OpenAI (для Whisper STT)
OPENAI_API_KEY=<ключ от OpenAI>

# Database (Railway автоматически предоставляет)
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/lobanov_bot

# App
APP_URL=https://lobanovo-bot-production.up.railway.app
APP_NAME=Lobanov AI Mentor
SECRET_KEY=change-me-to-random-string

# Настройки
DEFAULT_MODEL=grok-2
EMBEDDING_MODEL=google/gemini-embedding-001
MAX_VOICE_DURATION_SECONDS=300
MAX_MESSAGE_LENGTH=4000

# Прямая линия
DIRECT_LINE_PRICE_RUB=1000
DIRECT_LINE_WEEKLY_QUOTA=10
DIRECT_LINE_RESPONSE_DEADLINE_HOURS=48
DIRECT_LINE_AUTO_REFUND_HOURS=96
DIRECT_LINE_MAX_QUESTION_VOICE_SECONDS=180
